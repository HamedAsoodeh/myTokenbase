import nextConfig {
  const nextConfig = false,
    reactStrictMode: false,
    
}
import Jscofig = {
  const jsconfig = {
    first js = 55`;
  }
}
const nextConfig = {
 FirstLasttenn <= module.exports = 
   }  
.
